/**
 * Stage 0 — Project Input (Create Project form).
 * User-authored; never AI-generated.
 */

export interface ProjectInput {
  projectName: string;
  productIdea: string;
  targetUser: string;
  problem: string;
  businessGoal: string;
  constraints: string;
}
